# source/es — Material de traducción propio

- `videos/`: subtítulos `.srt` de las cinemáticas + `GENERAR_VIDEOS.bat`
  (los quema con ffmpeg sobre la copia del juego del usuario).

El texto del juego se tradujo directamente sobre los `.bra`; no se mantienen CSV intermedios.
